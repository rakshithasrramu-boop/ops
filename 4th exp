Experiment No:4

→sudo apt update
→sudo apt install apt transport-https co-certificates curl software-properties-Common-y
→sudo apt install docker.io -y
→sudo systemctl start decker
→sudo systemctl enable docker.
→docker --version
→sudo docker run hello-world
→mkdir my-python-app
→cd my-python-app
→touch app.py
nano app.py
cat app.py
→ touch Dockergale
nano dockerfile
→touch requirements.txt
cat requirements.txt
sudo systemctl start docker.
→docker build -t my-app-image
→sudo docker images
→docker run -d-p 5000: 5000 --name myapp-Containerd my-app-image
→ sudo docker ps 
→https://localhost: 5000

http://app.py
→ from flask import Flask
app = Flask(__name__)
@app.route('/')
def home():
    return "hello docker is running this python app successfully"
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)


FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py
EXPOSE 5000
CMD ["python", "app.py"]

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin url
git push -u origin master
